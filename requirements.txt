# Core TTS (pulls transformers, accelerate, torchaudio, soundfile, etc.)
qwen-tts==0.1.1

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12

# Audio encoding helpers (wav/flac/pcm via soundfile; mp3/opus/aac via ffmpeg)
numpy>=1.26.0
